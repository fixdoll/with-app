name = "submit-collab"
main = "submit-collab.js"
compatibility_date = "2024-01-01"

# Secrets (set via `wrangler secret put GITHUB_TOKEN`, never committed here):
#   GITHUB_TOKEN — a fine-grained GitHub PAT scoped to Issues: write on the
#   with-app repo only. Nothing else.
